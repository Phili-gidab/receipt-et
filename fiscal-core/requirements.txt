# Receipt fiscal-core — pinned runtime + dev dependencies.
# Stack: Python 3.13, FastAPI, SQLAlchemy 2.0, pydantic v2.
# requests is intentionally SYNC for byte-exact parity with the validated
# Delta SPMU eims.py code (do not swap for httpx/async).

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
cryptography>=42.0
requests>=2.32,<3.0
psycopg[binary]>=3.2,<4.0
boto3>=1.34
qrcode[pil]>=7.4
alembic>=1.13,<2.0
pytest>=8.2
python-dotenv>=1.0

# --- SaaS web app (server-rendered POS UI; lightweight for the 2 Mbps host) ---
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
