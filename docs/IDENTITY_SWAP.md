# Delta → Receipt identity swap register

Delta's identity was **borrowed scaffolding** to validate the platform against the
MoR sandbox (proven 2026-07-06: IRN `test-b38b…1b55`, RRN `test-f19e…2089`). When
Receipt's own onboarding completes (see `INSA_CERT_REQUEST.md` steps 0-5), every
item below gets replaced. **Verified 2026-07-06: zero Delta values exist in `app/`
code or templates** — only provenance comments. The swap is data + secrets only.

## The register

| # | Delta artifact (today) | Lives at | Receipt replacement | How |
|---|---|---|---|---|
| 1 | TIN `0107184904` | DB `merchants.tin` (+ scripts) | Receipt's company TIN | new merchant row / `POST /merchants` |
| 2 | Legal name `DELTA AESTHETICS` | DB `merchants.legal_name` | Receipt's registered legal name (exact MoR spelling) | same |
| 3 | System Number `B3D3D9DC50` | DB `merchants.system_number` | Receipt's approved source System Number | from MoR portal Source Management |
| 4 | client_id / client_secret / api_key | `.env` `DELTA_EIMS_*` → refs in `merchant_secrets` | Receipt's own credentials (issued with source approval) | new env vars / Secrets Manager entries + refs |
| 5 | INSA certificate (CN=0107184904, exp **2026-09-17**) | `secrets/delta/certificate.pem` | Receipt's INSA cert (CN=Receipt TIN) | `make_insa_csr.py --slug receipt` → ica@insa.gov.et |
| 6 | Private key (fetched from Delta EC2) | `secrets/delta/private_key.key` | Receipt's own key (generated locally, never travels) | generated by the CSR tool |
| 7 | VAT number `43256663343256663322` (sandbox seed) | `.env` / `merchants.vat_number` | Receipt's VAT registration number | env/row update |
| 8 | Seller block (Region 1 · City 101 · Wereda 13) | `merchants.*` | Receipt's address **exactly as MoR records it** (expect a 7017/1028 calibration round like Delta's) | row update + one sandbox probe |
| 9 | Default buyer ID `NID 3333367896666` (sandbox) | `.env` / `merchants.default_buyer_*` | remove for production (real buyer IDs per sale) | row update |
| 10 | Invoice chain (counter 39, last_irn `test-b38b…1b55`) | DB `invoice_chain` | Receipt starts fresh at 0 (its own sequence at MoR) | new row auto-created |
| 11 | Registered documents (Delta's test IRN/RRN rows) | DB `documents` | keep as evidence; Receipt's docs accrue separately per merchant | nothing to do |
| 12 | `scripts/seed_delta_merchant.py` | scripts | `seed_receipt_merchant.py` twin (or plain `POST /merchants`) | copy + swap constants |
| 13 | `scripts/sandbox_smoke.py` (`DELTA_TIN` constant) | scripts | parameterize `--tin` or a Receipt twin | small edit |
| 14 | Login phone `251951777888` (webapp dev login) | DB `merchants.phone` (currently None) | Receipt operator/merchant phones + real SMS OTP gateway | onboarding flow |

## What does NOT change

- `app/` — crypto, transport, builders, registration, routers, templates: **zero edits**.
- The contract spec, tests (62), Docker image, Terraform, docker-compose.
- The onboarding mechanics themselves — swapping Delta for Receipt is literally the
  same `POST /merchants` + secret-refs flow every future customer goes through.

## Post-swap hygiene

1. **Rotate Delta's sandbox credentials** (client_secret/api_key were shared in chat)
   and delete `secrets/delta/private_key.key` from this PC once Delta is re-onboarded
   as a normal customer under its own refreshed identity.
2. **Resync Delta's server chain** if its own EIMS integration re-enables (its config
   still thinks counter 38; sandbox is now at 39).
3. Diarize **INSA cert renewals** (~90-day validity; Delta's expires 2026-09-17).
4. `SESSION_SECRET` + real SMS OTP before any public deployment.
